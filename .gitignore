# Gradle
.gradle/
build/
**/test-results/
!gradle/wrapper/gradle-wrapper.jar

# Local configuration (SDK path, secrets)
local.properties

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws
.DS_Store

# Claude Code local session settings
.claude/settings.local.json

# On-device logcat captures from debug walkthroughs — session artefacts
/logcat_run_*.txt

# Android
captures/
*.apk
*.aab
*.keystore
*.jks
*.hprof
!debug.keystore

# Release signing secrets (see docs/ops/RELEASE_SIGNING.md) — never committed
keystore.properties

# Firebase config (contains API keys) — never committed
google-services.json

# Kotlin
.kotlin/

# Local design reference material (user-supplied, ~180MB including the
# original community zips under design-refs/archives/) — never committed
design-refs/
/*.zip

# Code-review working notes & scratch artefacts
/codex_review*.md
/52_issues_solve_progress*.md
*.pdf
bin/
*.log

