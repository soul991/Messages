<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ (android:dataExtractionRules). Both transports are default-denied
  (R-01): cloud backup AND device-to-device transfer. A device transfer is just
  as capable of copying the Room index, the cached secret-space KEK and the
  Drive master key as a cloud backup is.

  disableIfNoEncryptionCapabilities is deliberately NOT used — that would allow
  transfer on encrypted devices. Nothing here is transferable.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
